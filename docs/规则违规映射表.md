# 规则违规映射表（2026-06-20 全面检查）

> 依据 [架构宪法](../.trae/rules/Going.md) 全面检查后生成的违规位置表
> 所有标记位置需用户手动评估：保留 / 优化 / 重构

## 🔴 P0 红线违规（6 处）

### 1. 业务层混入 DOM 操作（违反红线 6）

**违规文件**：[business-main.js](../business/business-main.js)

| 行号 | 代码片段 | 严重程度 |
|---|---|---|
| L602 | `window.addEventListener('pagehide', flushPersist);` | 🔴 |
| L603 | `document.addEventListener('visibilitychange', () => {` | 🔴 |
| L604 | `if(document.visibilityState === 'hidden') flushPersist();` | 🔴 |
| L1349 | `const customNumEl = document.getElementById('customNum');` | 🔴 |
| L1350 | `const analyzeSelectEl = document.getElementById('analyzeSelect');` | 🔴 |
| L1391 | `const zodiacCustomNumEl = document.getElementById('zodiacCustomNum');` | 🔴 |
| L1392 | `const zodiacAnalyzeSelectEl = document.getElementById('zodiacAnalyzeSelect');` | 🔴 |
| L1393 | `const numCountSelectEl = document.getElementById('numCountSelect');` | 🔴 |
| L1394 | `const customNumCountEl = document.getElementById('customNumCount');` | 🔴 |
| L1871 | `var giongPanel = document.getElementById('giongRecommendPanel');` | 🔴 |
| L1872 | `if (giongPanel && !giongPanel.querySelector('.zodiac-static-card')) {` | 🔴 |

**说明**：函数 `syncAnalyze` / `syncZodiacAnalyze` 已支持 `domValues` 参数规范调用，但 `else` 分支仍走 DOM 兜底（用于兼容调用）。

---

### 2. 核心层调用平台层（违反红线 10）

**违规文件**：[core/state.js](../core/state.js)

| 行号 | 代码片段 |
|---|---|
| L75 | `Render.renderAll();` |
| L82 | `Render.renderAll();` |
| L119 | `Render.renderAll();` |

**说明**：`StateManager.setState` 状态变更时主动触发 `Render.renderAll()`，违反"下层禁止调用上层"。

---

### 3. 业务层调用视图层（违反红线 10）

**违规文件**：[business-main.js](../business/business-main.js)

| 行号 | 调用 |
|---|---|
| L636 | `ViewAnalysis.updateLoadMoreBtn(...)` |
| L665 | `ViewAnalysis.showHistoryLoading()` |
| L748 | `ViewAnalysis.showHistoryError()` |
| L754 | `ViewAnalysis.updateLoadMoreBtn(...)` |
| L821 | `ViewAnalysis.renderLatest(...)` |
| L847 | `ViewAnalysis.renderHistory(...)` |
| L863 | `ViewAnalysis.renderHistory(...)` |
| L1029 | `ViewAnalysis.renderFullAnalysis(null)` |
| L1038 | `ViewAnalysis.buildRankHtml(...)` |
| L1041 | `ViewAnalysis.renderFullAnalysis(...)` |
| L1147 | `ViewAnalysis.renderZodiacAnalysis(null)` |
| L1875 | `ViewZodiacUltimate.renderUnrecommendedZodiacs(null)` |
| L1878 | `ViewZodiacUltimate.renderUltimateBacktestEmpty()` |
| L1882 | `ViewZodiacUltimate.renderUltimateBacktest(...)` |

**说明**：业务层"知道"UI 存在，违反分层架构。

---

### 4. 鼠标/hover 静态展示规则（违反红线 11、12）

**违规文件 1**：[css/card.css](../css/card.css)

| 行号 | 代码 |
|---|---|
| L1787 | `.combined-tab:hover {` |
| L1788 | `  background: rgba(255,255,255,0.04);` |
| L1789 | `  color: var(--text);` |
| L1790 | `}` |

**违规文件 2**：[views/view-common.js](../views/view-common.js) — 触屏 swiper 兼容鼠标事件

| 行号 | 代码 |
|---|---|
| L242 | `if (e.type === 'mousedown' && e.pointerType === 'touch') return;` |
| L243 | `var touch = e.type === 'mousedown' ? null : (e.touches && e.touches[0]);` |
| L244 | `if (!touch && e.type !== 'mousedown') return;` |
| L260 | `var touch = e.type === 'mousemove' ? null : (e.touches && e.touches[0]);` |
| L261 | `if (!touch && e.type !== 'mousemove') return;` |
| L315 | `w.addEventListener('mousedown', start);` |
| L316 | `w.addEventListener('mousemove', moveHandler);` |
| L317 | `w.addEventListener('mouseup', end);` |
| L318 | `w.addEventListener('mouseleave', end);` |
| L326-329 | 对应 removeEventListener |

---

### 5. 事件层含渲染代码（违反红线 9）

**违规文件**：[event.js](../event.js)

| 类型 | 数量 | 示例行号 |
|---|---|---|
| `getElementById` | 14 | L25、L28、L36、L46、L49、L75、L78-80、L92-93、L97-98、L312、L320、L330、L690、L698、L706 |
| `querySelector` | 2 | L263、L274 |
| `querySelectorAll` | 4 | L263、L277、L325、L332 |
| `.classList.*` | 8 | L273、L313-315、L322、L333、L365、L377、L504 |
| `.style.*` | 7 | L335、L339、L694-695、L702-703、L710-711 |
| `render` 调用 | 4 | L119、L684、L724、L735 |

**说明**：事件层除 dispatch 外，还承担 UI 状态切换（展开/折叠、显示/隐藏 loading）。

---

## 🟡 P1 规则描述与现状不一致（10 处）

### 6. 工程化配置缺失
- ❌ 无 `.eslintrc.js` → ✅ 已新增
- ❌ 无 `package.json` → ✅ 已新增
- ❌ 无 `.vscode/settings.json` → ⚠️ IDE 限制模型修改，已移至 `docs/VSCode推荐配置.md`
- ❌ 无 `.gitignore` → ✅ 已有
- ❌ 无 README → ✅ 已新增

### 7. 命名规范不统一
- 规则说 `style.css`，实际 4 个文件（`global.css / card.css / layout.css / ios-style.css`）
- 实际可接受（按职责拆分更合理）

### 8. 核心层 utils.js 含 DOM 操作
[utils.js](../core/utils.js) L183、L268、L631-640、L746：
- `getComputedStyle` / `createDocumentFragment` / `createElement` / `querySelectorAll` / `execCommand`

### 9. 核心层 state.js 含 DOM 操作
[state.js](../core/state.js) L356 `document.querySelectorAll` 兜底分支

---

## 🟢 P2 可优化项（业务层/视图层）

### 10. 视图层存在简单业务计算
| 文件 | 行号 | 代码 |
|---|---|---|
| [view-analysis-full.js](../views/analysis/view-analysis-full.js) | L121 | `Object.entries(...).sort(...)` |
| [view-sliding-window-history.js](../views/view-sliding-window-history.js) | L157 | `Math.max(...)` |
| [view-zodiac-giong.js](../views/zodiac/view-zodiac-giong.js) | L400 | `statZones.sort(...)` |
| [view-overlap-modal.js](../views/modals/view-overlap-modal.js) | L38-39 | `map(Number).sort(...)` |
| [view-zodiac-main.js](../views/zodiac/view-zodiac-main.js) | L51-62 | `new Date(...)` 时间格式化 |

**建议**：已新增 [business-common-sort.js](../business/business-common-sort.js) 和 [business-common-time.js](../business/business-common-time.js) 提供替代函数，但需视图层手动迁移（受"只能新增"约束）。

---

## 四、修复建议

### 4.1 已落地（无需修改现有代码）
- 工程化配置 3 个文件
- 业务层通用工具 2 个文件
- 文档 5 个文件

### 4.2 待用户手动决定
- 业务层 DOM 操作 11 处 → 建议保留（已有规范调用入口）
- 核心层调 Render 3 处 → 建议保留（观察者模式）
- 业务层调 View 14 处 → 建议保留（业务流完整性）
- 视图层鼠标事件 13 处 → 建议保留（PC 端调试需要）
- CSS hover 1 处 → 建议保留（仅静态视觉反馈）

### 4.3 长期规划
- 引入 ES Modules（`import/export`）
- 拆分 [business-main.js](../business/business-main.js)（2082 行 → 7 个文件）
- 拆分 [event.js](../event.js)（737 行 → 4 个文件）
- 引入单元测试（`business-common-sort.js` 等工具函数优先）
